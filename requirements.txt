# FastAPI backend requirements for ExpressDiff (Python 3.6 compatible)
# Core FastAPI and web server
fastapi==0.68.2
uvicorn==0.15.0

# Data validation and serialization  
#pydantic==1.10.12
pydantic==1.8.2

# Data processing (for existing pipeline scripts)
# NOTE: pandas 1.3.x requires Python >=3.7. Cluster environment currently uses Python 3.6,
# so we pin to the last series supporting 3.6.
pandas==1.1.5
numpy==1.19.5

# PyDESeq2 for differential expression analysis (may need newer Python)
# pydeseq2==0.4.6  # Commented out - requires Python 3.8+

# File handling
python-multipart==0.0.5
aiofiles==0.7.0

# Additional utilities
python-dotenv==0.19.2

# Typing extensions for Python 3.6
typing-extensions==4.1.1

# Optional: for enhanced logging
# loguru==0.7.2  # May not be compatible with Python 3.6

# Note: Some packages may require newer Python versions
# Consider using a newer Python module if available on HPC